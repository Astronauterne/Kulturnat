---

---

<nav>
	<div class='menuOverlay'>
		<ul>
			<li><a href='/'>FORSIDE</a></li>
			<li><a href='http://'>PROGRAM</a></li>
			<li><a href='http://'>OM KULTURNATTEN</a></li>
			<li><a href='http://'>BLIV ARRANGØR</a></li>
		</ul>
	</div>
</nav>

<style>
	nav {
		position: fixed;
		z-index: 2;
		width: 100vw;
	}
	.menuOverlay {
		background-color: var(--menu-bg);
		height: 100vh;
		width: 100vw;
		top: 0;
		left: 0;
		z-index: 1;
		position: fixed;
		display: none;
		flex-direction: column;
		justify-content: center;
	}

	.menuOverlay.active {
		display: flex;
	}

	.menuOverlay ul {
		list-style: none;
		justify-content: center;
		margin: auto;
		padding-left: 10vw;
	}

	.menuOverlay li {
		padding-bottom: 2rem;
	}

	.menuOverlay a {
		position: relative;
		text-decoration: none;
		color: var(--heading-fg);
		font-family: 'Futura Medium';
		font-size: clamp(1rem, 10vw, 4rem);
		font-weight: 700;
	}

	.menuOverlay a:visited {
		color: var(--heading-fg);
	}

	.menuOverlay a::before {
		content: '';
		position: absolute;
		display: block;
		width: 100%;
		height: 4px;
		bottom: 0;
		left: 0;
		background-color: var(--heading-fg);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.menuOverlay a:hover::before {
		transform: scaleX(1);
	}

	@media (min-width: 800px) {
		.menuOverlay ul {
		padding-left: 0vw;
	}
	}
</style>
